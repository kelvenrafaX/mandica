import { Orcamento } from './orcamento';

export class Eventos {
  title?: string;
  start?: Date;
  end?: Date;
  backgroundColor?: string;
  borderColor?: string;
  orcamento?: Orcamento;
  date?: any;
}
